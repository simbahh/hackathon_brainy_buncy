import 'package:flutter/material.dart';

class AppColors {
  // Main colors
  static const Color primary = Color.fromARGB(255, 0, 0, 0); // Blue
  static const Color background = Colors.white;

  // Text colors
  static const Color textPrimary = Color(0xFF424242); // Dark grey
  static const Color textSecondary = Color(0xFF757575); // Medium grey

  // Status colors
  static const Color success = Color(0xFF4CAF50); // Green
  static const Color error = Color(0xFFf44336); // Red

  // UI Element colors
  static const Color cardBorder = Color(0xFFE0E0E0); // Light grey
  static const Color shadow = Color(0x1A000000); // Black with opacity

  // Button colors
  static const Color buttonBackground = primary;
  static const Color buttonText = Colors.white;
}
